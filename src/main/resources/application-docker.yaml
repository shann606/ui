spring:
  application:
    name: ui frontend

server:
  port: 4000

logging:
  level:
    root: warn
    '[org.springframework.web]': info
    '[com.exp.ui]': trace
eureka:
  client:
    service-url:
      defaultZone : http://host.docker.internal:8761/eureka



    
    
